# generated from bayt.cue — do not edit
include:
- path: ./compose.bayt.yaml
  required: false
services:
  setup:
    extends:
      file: ./compose.setup.yaml
      service: plugins_pronto-setup
    profiles:
    - setup
  build:
    extends:
      file: ./compose.build.yaml
      service: plugins_pronto-build
    profiles:
    - build
